[project]
name = "fastflow"
version = "0.4.0"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4",
    "ddgs",
    "markdownify",
    "openai",
    "playwright",
    "requests",
    "selenium",
]
